model:
  input_width: 256
  input_height: 256
  input_channels: 3
  base_channels: 16
  output_channels: 1
  dropout: 0.1

training:
  epochs: 50
  batch_size: 4
  learning_rate: 0.001
  optimizer: adam
  early_stopping_patience: 8
  reduce_lr_patience: 4
  checkpoint_metric: val_dice_score

